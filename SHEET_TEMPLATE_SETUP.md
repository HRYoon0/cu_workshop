# 사용자별 시트 자동 생성 가이드

각 사용자가 자신만의 구글 시트를 자동으로 가져가는 방법입니다.

## 🎯 핵심 아이디어

1. **원본 템플릿 시트 1개** 생성 (관리자가 한 번만)
2. **사용자 로그인 시 자동으로 사본 생성**
   - "교육과정 워크숍" 폴더에 저장
   - Apps Script도 자동으로 복사됨
3. **각 사용자는 자신의 시트만 사용**
   - 다른 사용자 데이터와 분리
   - 15GB 개인 Drive 용량 사용

## 📋 1단계: 템플릿 시트 생성 (관리자만, 한 번)

### 1.1 새 구글 스프레드시트 생성

1. [Google Sheets](https://sheets.google.com) 접속
2. **빈 스프레드시트** 생성
3. 이름: "워크숍 결과 템플릿"

### 1.2 Apps Script 코드 추가

`GOOGLE_SHEETS_SETUP.md` 파일의 Apps Script 코드를 그대로 복사하여:

1. 확장 프로그램 → Apps Script
2. 코드 붙여넣기 → 저장
3. 구글 시트로 돌아가기 → 새로고침
4. 🎓 워크숍 설정 → 📊 시트 구조 자동 생성
5. ✅ "퀴즈결과", "설문결과" 시트 생성 확인

### 1.3 웹 앱 배포 (템플릿용)

1. Apps Script 탭으로 이동
2. 배포 → 새 배포 → 웹 앱
3. 설정:
   - 실행: **나**
   - 액세스 권한: **모든 사용자**
4. 배포 클릭 → 권한 승인
5. **웹 앱 URL 복사하지 말고 닫기** (사용자별로 다시 배포함)

### 1.4 템플릿 시트 ID 가져오기

구글 시트 URL에서 ID 복사:
```
https://docs.google.com/spreadsheets/d/[여기가_시트_ID]/edit
```

예시:
```
1a2B3c4D5e6F7g8H9i0J1k2L3m4N5o6P7q8R9s0T
```

### 1.5 템플릿 ID 환경 변수에 추가

`.env` 파일에 추가:
```env
NEXT_PUBLIC_SHEET_TEMPLATE_ID=복사한_템플릿_시트_ID
```

## 🔄 2단계: 사용자별 자동 복사 (앱에서 자동 처리)

### 작동 방식:

1. **사용자 첫 로그인**
   - 템플릿 시트 복사
   - "교육과정 워크숍" 폴더에 저장
   - 시트 이름: `워크숍결과_사용자이름_2024-01-15`
   - Apps Script도 자동으로 복사됨!

2. **사용자 시트 정보 저장** (Firestore)
   - 시트 ID
   - 시트 URL
   - 웹 앱 URL (배포 후 추가)

3. **데이터 기록**
   - 사용자의 퀴즈/설문 결과가 자신의 시트에만 기록됨

## 🚀 3단계: 사용자 앱 배포 (각 사용자가 한 번씩)

**문제점**: Apps Script는 복사되지만, 웹 앱은 수동 배포 필요

**해결 방법**: 관리자 페이지에서 "시트 설정" 버튼 제공

### 자동화 흐름:

1. 사용자 로그인
2. 시트가 없으면 자동 생성 → "교육과정 워크숍" 폴더에 저장
3. 관리자 페이지 상단에 안내 메시지 표시:
   ```
   ⚠️ 시트 배포 필요: 데이터를 자동으로 기록하려면 한 번만 설정하세요
   [시트 열기] [나중에]
   ```
4. "시트 열기" 클릭 → 새 탭에서 사용자 시트 열림
5. 사용자가 직접:
   - 확장 프로그램 → Apps Script
   - 배포 → 새 배포 → 웹 앱
   - 실행: **나** / 액세스: **모든 사용자**
   - 웹 앱 URL 복사
6. 관리자 페이지로 돌아와서 URL 붙여넣기 → 저장

## 📁 폴더 구조

```
내 드라이브/
└── 교육과정 워크숍/
    ├── 이미지/
    │   ├── quiz-image-xxx.jpg
    │   └── survey-image-xxx.png
    └── 워크숍결과_김선생_2024-01-15
        ├── 퀴즈결과 (시트)
        └── 설문결과 (시트)
```

## 🎁 장점

✅ **데이터 분리**: 각 사용자가 자신의 데이터만 관리
✅ **용량 걱정 없음**: 개인 Drive 15GB 사용
✅ **템플릿 보호**: 원본 시트는 변경되지 않음
✅ **Apps Script 자동 복사**: 코드 재입력 불필요
✅ **폴더 자동 정리**: "교육과정 워크숍" 폴더에 자동 저장

## ⚠️ 주의사항

### 템플릿 시트 수정

템플릿 시트를 수정하려면:
1. 템플릿 시트 수정 (구조, Apps Script 등)
2. 사용자들은 이미 생성된 시트 사용
3. 새 사용자만 수정된 템플릿 받음

### 기존 사용자 시트 재생성

사용자 시트를 다시 만들려면:
1. Firestore의 `userSheets` 컬렉션에서 해당 사용자 문서 삭제
2. 사용자가 다시 로그인하면 새 시트 생성

## 🔒 보안

- 템플릿 시트: 링크를 아는 사람만 볼 수 있음 (권장)
- 사용자 시트: 각 사용자만 접근 가능
- Drive API: `drive.file` 스코프만 사용 (검증 불필요)

## 📊 데이터 백업

각 사용자의 시트는 Google Drive에 자동 백업됩니다.
추가 백업이 필요하면 구글 시트 → 파일 → 다운로드 → Excel

## 🆘 문제 해결

### 시트가 복사되지 않음
- 템플릿 ID가 올바른지 확인
- Drive API가 활성화되었는지 확인

### "교육과정 워크숍" 폴더가 보이지 않음
- 내 드라이브 검색: "교육과정 워크숍"
- 자동 생성되므로 기다려보세요

### Apps Script가 작동하지 않음
- 시트에서 확장 프로그램 → Apps Script 확인
- 코드가 복사되었는지 확인
- 웹 앱을 배포했는지 확인
