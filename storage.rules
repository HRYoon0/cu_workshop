rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 모든 파일 읽기 허용 (이미지 공개 접근용)
    match /{allPaths=**} {
      allow read;
      allow write: if request.auth != null;
    }
  }
}
