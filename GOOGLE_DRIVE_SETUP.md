# Google Drive API 설정 가이드

이 가이드를 따라 Google Drive API를 활성화하고 이미지 자동 업로드 및 시트 생성 기능을 설정하세요.

## Drive 폴더 구조

파일들이 자동으로 다음과 같은 구조로 저장됩니다:

```
내 드라이브/
└── 교육과정 워크숍/
    ├── 이미지/
    │   ├── quiz-image-xxxxx.jpg
    │   └── quiz-image-xxxxx.png
    └── 퀴즈결과_2024-01-15.xlsx
    └── 설문결과_2024-01-15.xlsx
```

## 1단계: Google Cloud Console에서 API 활성화

1. **Google Cloud Console 접속**
   - https://console.cloud.google.com/
   - Firebase 프로젝트와 동일한 프로젝트 선택 (`cu-workshop-158c0`)

2. **Google Drive API 활성화**
   - 왼쪽 메뉴 → `API 및 서비스` → `라이브러리`
   - 검색창에 "Google Drive API" 입력
   - `Google Drive API` 클릭
   - `사용` 버튼 클릭
   - ✅ **이것만 활성화하면 됩니다!** (시트 생성 포함)

## 2단계: OAuth 2.0 클라이언트 ID 생성

1. **OAuth 동의 화면 설정**
   - 왼쪽 메뉴 → `API 및 서비스` → `OAuth 동의 화면`
   - 사용자 유형: `외부` 선택 → `만들기`
   - 앱 정보:
     - **앱 이름**: `교육과정 워크숍 퀴즈`
     - **사용자 지원 이메일**: 본인 이메일
     - **개발자 연락처 정보**: 본인 이메일
   - `저장 후 계속` 클릭

2. **범위 추가** (2단계)
   - `범위 추가 또는 삭제` 클릭
   - 필터에서 "drive" 검색
   - `.../auth/drive.file` 선택 (업로드한 파일만 접근)
   - ✅ **이것만 추가하면 됩니다!** (이미지 + 시트 생성 모두 가능)
   - ⚠️ **spreadsheets 스코프는 추가하지 마세요** (검증 필요)
   - `업데이트` → `저장 후 계속`

3. **테스트 사용자 추가** (3단계)
   - 건너뛰기 또는 본인 이메일만 추가
   - `저장 후 계속`

4. **OAuth 클라이언트 ID 생성**
   - 왼쪽 메뉴 → `API 및 서비스` → `사용자 인증 정보`
   - `+ 사용자 인증 정보 만들기` → `OAuth 클라이언트 ID`
   - 애플리케이션 유형: `웹 애플리케이션`
   - 이름: `워크숍 퀴즈 웹 클라이언트`
   - **승인된 JavaScript 원본**:
     - `http://localhost:3000`
     - 배포 후 실제 도메인 추가
   - **승인된 리디렉션 URI**:
     - `http://localhost:3000`
     - 배포 후 실제 도메인 추가
   - `만들기` 클릭

5. **클라이언트 ID 복사**
   - 생성된 팝업에서 **클라이언트 ID** 복사
   - `.env` 파일에 추가:
     ```
     NEXT_PUBLIC_GOOGLE_DRIVE_CLIENT_ID=복사한_클라이언트_ID
     ```

## ⚠️ 이미 spreadsheets 스코프를 추가했다면?

**경고 메시지를 없애려면:**

1. Google Cloud Console → `API 및 서비스` → `OAuth 동의 화면`
2. `앱 수정` 클릭
3. 2단계 (범위) → `범위 추가 또는 삭제`
4. `.../auth/spreadsheets` 체크 해제
5. `.../auth/drive.file`만 남기고 저장
6. 경고 메시지가 사라집니다! ✅

## 완료!

설정이 완료되면 코드에서 Google Drive에 자동으로 이미지를 업로드할 수 있습니다.

## 주의사항

### 시트 생성 방법

**좋은 소식! 🎉**
- 시트 생성은 **Drive API만으로 가능**합니다
- `drive.file` 스코프만 있으면 됩니다
- **검증 불필요, 테스트 사용자 추가 불필요**
- 누구나 바로 사용 가능!

### 일반 주의사항

- OAuth 동의 화면은 "테스트" 모드로 유지
- `drive.file` 스코프만 사용하므로 검증 불필요
- API 사용량 제한: 하루 10억 쿼리 (무료)
- Drive 저장 공간: 각 사용자의 개인 Drive 용량 사용 (15GB)
- 모든 사용자가 로그인 가능 (테스트 사용자 추가 불필요)
