# Google Drive API 설정 가이드

이 가이드를 따라 Google Drive API를 활성화하고 이미지 자동 업로드 기능을 설정하세요.

## 1단계: Google Cloud Console에서 API 활성화

1. **Google Cloud Console 접속**
   - https://console.cloud.google.com/
   - Firebase 프로젝트와 동일한 프로젝트 선택 (`cu-workshop-158c0`)

2. **Google Drive API 활성화**
   - 왼쪽 메뉴 → `API 및 서비스` → `라이브러리`
   - 검색창에 "Google Drive API" 입력
   - `Google Drive API` 클릭
   - `사용` 버튼 클릭

## 2단계: OAuth 2.0 클라이언트 ID 생성

1. **OAuth 동의 화면 설정**
   - 왼쪽 메뉴 → `API 및 서비스` → `OAuth 동의 화면`
   - 사용자 유형: `외부` 선택 → `만들기`
   - 앱 정보:
     - **앱 이름**: `교육과정 워크숍 퀴즈`
     - **사용자 지원 이메일**: 본인 이메일
     - **개발자 연락처 정보**: 본인 이메일
   - `저장 후 계속` 클릭

2. **범위 추가** (2단계)
   - `범위 추가 또는 삭제` 클릭
   - 필터에서 "drive" 검색
   - `.../auth/drive.file` 선택 (업로드한 파일만 접근)
   - `업데이트` → `저장 후 계속`

3. **테스트 사용자 추가** (3단계, 선택사항)
   - 개발 중에는 본인 이메일만 추가
   - `저장 후 계속`

4. **OAuth 클라이언트 ID 생성**
   - 왼쪽 메뉴 → `API 및 서비스` → `사용자 인증 정보`
   - `+ 사용자 인증 정보 만들기` → `OAuth 클라이언트 ID`
   - 애플리케이션 유형: `웹 애플리케이션`
   - 이름: `워크숍 퀴즈 웹 클라이언트`
   - **승인된 JavaScript 원본**:
     - `http://localhost:3000`
     - 배포 후 실제 도메인 추가
   - **승인된 리디렉션 URI**:
     - `http://localhost:3000`
     - 배포 후 실제 도메인 추가
   - `만들기` 클릭

5. **클라이언트 ID 복사**
   - 생성된 팝업에서 **클라이언트 ID** 복사
   - `.env` 파일에 추가:
     ```
     NEXT_PUBLIC_GOOGLE_DRIVE_CLIENT_ID=복사한_클라이언트_ID
     NEXT_PUBLIC_GOOGLE_API_KEY=복사한_API_키
     ```

## 3단계: API 키 생성 (선택사항)

1. **API 키 생성**
   - `+ 사용자 인증 정보 만들기` → `API 키`
   - 생성된 API 키 복사
   - `.env` 파일에 추가

## 완료!

설정이 완료되면 코드에서 Google Drive에 자동으로 이미지를 업로드할 수 있습니다.

## 주의사항

- OAuth 동의 화면이 "테스트" 모드일 경우, 추가한 테스트 사용자만 사용 가능합니다
- 실제 배포 시 "프로덕션" 모드로 전환하거나 사용자를 추가해야 합니다
- API 사용량 제한: 하루 10억 쿼리 (무료)
- Drive 저장 공간: 각 사용자의 개인 Drive 용량 사용 (15GB)
